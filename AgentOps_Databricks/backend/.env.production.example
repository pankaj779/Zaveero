# Production — set in Render dashboard

JWT_SECRET=generate-with-openssl-rand-hex-32
ZAAVERO_API_URL=https://api.zaavero.com

# Comma-separated frontend origins (localhost added automatically in dev)
CORS_ORIGINS=https://agentops.zaavero.com

# Optional server-side Databricks defaults (users configure per-workspace in UI)
# DATABRICKS_HOST=
# DATABRICKS_TOKEN=
# AGENTOPS_INFERENCE_SCHEMA=
